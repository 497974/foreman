[pytest]
; Foreman's own suite lives in tests/. The runs/ tree contains per-run agent
; workspaces whose generated test files belong to *their* projects — pytest
; must not collect them when testing Foreman itself.
testpaths = tests
norecursedirs = runs .git __pycache__
