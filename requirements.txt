# Qwen via DashScope (OpenAI-compatible mode) — the only runtime dependency.
# The orchestration core (ledger/dispatcher/state machine) is stdlib-only by
# design: deterministic scheduling should not need a framework.
openai>=1.40.0

# Dev / test
pytest>=8.0.0
